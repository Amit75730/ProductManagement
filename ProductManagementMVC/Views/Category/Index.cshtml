@model List<CategoryDTO>

<h2>Categories</h2>
<ul>
    @foreach (var category in Model)
    {
        <li><a href="@Url.Action("Products", "Category", new { categoryId = category.CategoryId })">@category.Name</a></li>
    }
</ul>
